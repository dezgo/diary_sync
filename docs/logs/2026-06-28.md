# Session log — 2026-06-28

## Summary
Backfilled the 19 June diary transcript (music-blocked on YouTube) via a new local-Whisper fallback, cleaned it up, ran a duplicate-photo audit, swept a name misspelling, enriched 5 scans, and reorganised the Scans folder into an inbox + per-year archive.

## What was done

### Diary transcript (19 June 2026)
- `python sync.py --date 2026-06-19` returned "no transcript" — background music tripped a YouTube Content ID block that disables auto-captions.
- Built a **local Whisper fallback**: `yt-dlp -f 18` (format 18 required; SABR-only streaming strips audio-only formats) → `faster-whisper` `small.en` → injected via the project's own `format_transcript` + `update_note` so output matches a normal sync. Summary written by hand (no `ANTHROPIC_API_KEY` in env).
- Cleaned Whisper garbles; corrected proper nouns over several rounds (Derek spot-corrected): **Giovani, App Foundry / flowfield.appfoundry.cc, 88 (80s bar), ChatGPT, Jolimont Centre, Vicky**. Used `large-v3` to recover one fuzzy line; noted large-v3 is not always more accurate (it changed the correct "88" → "ADA").
- Removed 23 em dashes (Derek dislikes them).

### Duplicate-photo audit
- Scanned all 179 diary notes since Jan 2026. No same-file-twice and no one-image-two-names cases. One cross-day dupe: June 6's POTD was also in the 5 June note (which had no POTD). Removed the stray embed. Live code can't produce this routing → one-off, not a systemic bug.

### Vicky spelling sweep
- `Vicki`→`Vicky` across 61 Diary/2026 notes (193 occurrences), whole-word, backed up. Scoped to Diary/2026 — left unrelated "Vicki" (e.g. "Vicki Beard" in CW notes, 2017/2021 diary) untouched.

### Scans
- Enriched 5 pending scans (Ashley cervical screening reminder, Braun Series 5 manual, Shaver Shop receipt, **Tesla Certificate of Registration YSX67A**, Jaycar receipt). Fixed two wrong companion dates (`1111-11-11`→2026-05-20; `2027-06-25` rego *expiry*→2026-06-26 issue).
- **Reorganised Scans/ into inbox + archive**: added `scan_tools.file_away()` — companion `.md` → `Scans/<year>/`, PDF → `Scans/<year>/attachments/`; undated → `Scans/Reference/`. Buckets by the ISO date prefix in the filename. Swept all 27 existing enriched scans (18 → 2026/, 9 → Reference/). Verified all 27 embeds resolve (0 broken). Scans/ root now empty of loose files.

## Files changed (repo)
- `scan_tools.py` — added `file_away()`
- `.claude/skills/scan-enrich/SKILL.md` — added Step 6 (file_away / inbox-archive layout), `date=` tip, "don't re-run apply_enrichment on an enriched scan" guardrail
- `.claude/skills/diary-transcript-cleanup/SKILL.md` — NEW skill (transcript fix/backfill flow + known-misspellings table)
- `docs/logs/2026-06-28.md` — this log

## Memory (outside repo, in the memory dir)
- New: `project_music_blocked_transcripts.md`, `reference_diary_proper_nouns.md`, `feedback_no_em_dashes.md`, `project_scans_organisation.md`; MEMORY.md index updated.

## Decisions / conventions
- Scans **inbox model** (root = un-enriched only; enriched filed by year, PDFs in `attachments/`, undated → Reference). Safe because scan discovery is non-recursive.
- To change a scan's date use `apply_enrichment(..., date="YYYY-MM-DD")` — never apply then re-run (re-running corrupts the companion; it wiped the Jaycar embed this session and had to be hand-rebuilt).
- Don't split companion from PDF during enrichment — only at filing time.

## Gotchas
- Scan discovery is non-recursive (enables the archive subfolders).
- Obsidian resolves `![[name.pdf]]` by basename vault-wide → moving PDFs is safe.
- Obsidian file-explorer shows a stale cache after external bulk moves — needs Ctrl+R reload; filesystem was correct.
- `apply_enrichment` corrupts the companion if re-run on an already-enriched scan.
- Whisper models cached at `C:\Users\Derek\.cache\huggingface\hub\` (small.en ~464 MB, large-v3 ~3 GB), persist.

## Still TODO / open
- Derek to reload Obsidian to clear the stale Scans view.
- `pdf_note_tools.py` titles + OCR (~287 scans) still pending from prior sessions (untouched this session).
